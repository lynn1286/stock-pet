# 桌面宠物悬浮原理

以 claude-pet（Electron 实现）为例，说明如何让窗口浮在桌面又不干扰其他软件。

## 核心：四件事

### 1. 把窗口做成"鬼魂"

普通窗口有标题栏、有背景色、能接收键盘输入。创建窗口时把这些全关掉：

- `frame: false` → 没有标题栏
- `transparent: true` → 背景全透明，只有宠物图案有颜色
- `focusable: false` → 键盘输入永远不会跑到这个窗口里
- `hasShadow: false` → 无阴影
- `skipTaskbar: true` → 不出现在任务栏
- `backgroundColor: '#00000000'` → 全透明底色

配合 CSS `html, body { background: transparent; overflow: hidden; }`，只有宠物的 SVG/动画区域有像素，其余区域全透明——鼠标点击透明区域会穿透到下层窗口。

### 2. 让 macOS 忘掉这个应用

`app.setActivationPolicy('accessory')` 告诉 macOS："我不是一个正经应用。"

macOS 三种 activation policy：
- **regular**：普通应用，有 Dock 图标，能抢前台
- **accessory**：辅助应用，无 Dock 图标，不进 Cmd+Tab 列表，不抢前台
- **prohibited**：完全隐藏

accessory 让整个 Electron 进程变成"后台服务"，macOS 根本不把它当作可交互应用管理。

### 3. 让它永远浮在最上面

```js
win.setAlwaysOnTop(true, 'screen-saver')
```

`screen-saver` 是 macOS 窗口层级中最高的（比 normal、floating 都高），确保宠物浮在所有普通窗口之上。

再加上：
```js
win.setVisibleOnAllWorkspaces(true, { visibleOnFullScreen: true, skipTransformProcessType: true })
```

- 所有虚拟桌面（Space）都可见
- 别的应用全屏时也能浮在上面
- `skipTransformProcessType: true` 避免进程类型转换导致间歇性抢占前台

### 4. 拖拽不抢焦点

不用 Electron 自带的 `-webkit-app-region: drag`（会吞滚轮事件），而是 JS + IPC 手动接管：

1. 渲染层用 Pointer Events + `setPointerCapture` 追踪鼠标位移
2. 通过 IPC 告知主进程 `win.setPosition()` 移动窗口
3. `lostpointercapture` / `pointercancel` 兜底结束拖拽（切 App 时不会卡住）

全程窗口不获取焦点，被拖拽的应用不受影响。

## 一句话总结

把窗口的"存在感"全部关掉（无边框、透明、不抢焦点、不出现在任务栏），只留一个看得见但摸不着的图案浮在最上面。

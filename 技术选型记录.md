# Stock-Pet 技术选型记录

## 数据源：东方财富 API

选择理由：免认证、JSON 返回、A 股和基金都支持。

### 股票实时行情
```
GET http://push2.eastmoney.com/api/qt/stock/get?secid=1.600519&fields=f57,f58,f43,f170,f171
```
- secid 格式：`1.XXXXXX` = 沪市，`0.XXXXXX` = 深市
- f57=代码, f58=名称, f43=最新价, f170=涨跌幅, f171=涨跌额

### 基金实时估值（天天基金 JSONP）
```
GET http://fundgz.1234567.com.cn/js/000001.js
```
返回 `jsonpgz({...})`，需截取 JSON。`gsz`=估算净值, `gszzl`=估算涨跌幅。

### 其他备选数据源

| 数据源 | 认证 | 格式 | 稳定性 |
|---|---|---|---|
| 东方财富 | 无 | JSON | 较好 |
| 腾讯财经 | 无 | 文本(~分隔) | 较好 |
| 新浪财经 | Referer | 文本(逗号分隔) | 一般 |
| 雪球 | Cookie | JSON | 较好 |

## 窗口方案：Tauri v2

### 关键配置
- `transparent: true` + `macOSPrivateApi: true` → 透明背景
- `decorations: false` → 无边框
- `alwaysOnTop: true` → 置顶
- `focusable: false` → 不抢焦点
- `visibleOnAllWorkspaces: true` → 所有桌面可见

### macOS 隐藏 Dock 图标
通过 objc2 调用 `setActivationPolicy:1`（Accessory 模式）。`skipTaskbar` 在 macOS 上不生效。

### 点击穿透
`setIgnoreCursorEvents(true)` 默认开启，鼠标进入宠物区域时动态关闭。

### Tauri v2 踩坑
- `macOSPrivateApi: true` 导致无法上架 Mac App Store
- `skipTaskbar` 在 macOS 上不生效，需用 objc2
- `focusable: false` 后需要输入时需动态切换
- `visibleOnAllWorkspaces` 可能需要延迟设置才生效

## 动画方案：lottie-react

### 选择理由
- 底层 lottie-web（Airbnb 官方维护）
- TypeScript 类型内置
- 周下载量 700 万+

### 渲染器选择
桌面应用用 canvas 渲染器（DOM 节点少、帧率高、受益于 Chromium 硬件加速）。

### 状态切换技巧
用 `key={petState}` 强制 React 重新挂载 Lottie 组件，确保干净切换。

### 透明背景
原生支持，Lottie JSON 无背景层即可。Canvas 默认透明。
